// import { selectThemeProviderDomain } from '../selectors';

describe('selectThemeProviderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
