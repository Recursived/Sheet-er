/*
 * SpeedDialMenu Messages
 *
 * This contains all the text for the SpeedDialMenu component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SpeedDialMenu';

export default defineMessages({

  labeltheme: {
    id: `${scope}.labeltheme`,
    defaultMessage: 'Change theme',
  },

  labelsheet: {
    id: `${scope}.labelsheet`,
    defaultMessage: 'Create sheet',
  },

  labelprofil: {
    id: `${scope}.labelprofil`,
    defaultMessage: 'My profil',
  },

  labeloptions: {
    id: `${scope}.labeltheme`,
    defaultMessage: 'Options',
  },
});
