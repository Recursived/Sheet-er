/*
 * SheeterNav Messages
 *
 * This contains all the text for the SheeterNav component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SheeterNav';

export default defineMessages({
  
});
