import styled from 'styled-components';

/**
 * Clickable item used it the dropdownlist
 */
export const ClickableItem = styled.span`
  cursor: pointer;
  &:hover {
      text-decoration: underline;
  }
`;