/*
 * NavButtonLogin Messages
 *
 * This contains all the text for the NavButtonLogin component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NavButtonLogin';

export default defineMessages({
  loginbutton: {
    id: `${scope}.loginbutton`,
    defaultMessage: 'Log into Sheeter',
  },
});
