/*
 * DashboardCard Messages
 *
 * This contains all the text for the DashboardCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DashboardCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardCard component!',
  },
});
