/*
 * ModalContact Messages
 *
 * This contains all the text for the ModalContact component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ModalContact';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ModalContact component!',
  },
});
