/*
 * ThemeToggler Messages
 *
 * This contains all the text for the ThemeToggler component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ThemeToggler';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Change theme',
  },
});
